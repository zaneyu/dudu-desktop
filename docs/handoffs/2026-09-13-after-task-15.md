# Dudu Windows handoff after Task 15 and the owner-voice copy pass

Date: 2026-09-13  
Branch: `codex/windows-v1`  
Head: `bd9376d` (`fix: finish voice pass on privacy and pairing text`)  
Next planned work: Task 16, version-one cross-platform encryption protocol (plan line 1838)

## Current status

Tasks 1 through 15 in `docs/superpowers/plans/2026-09-11-dudu-desktop-companion-implementation.md` are complete. Do not restart or reimplement Task 15.

Commits since the Task 14 handoff (`a9163ae`):

- `c2503cc` `feat: enforce private calm presentation policy` (Task 15 milestone)
- `c37523f` `fix: serialize presentation gateway state` (Task 15 review fixes)
- `fab0507`, `e997f04`, `bd9376d` owner-voice display-text pass (user request outside the numbered plan)

Task 15 passed a Sonnet spec/quality review after one fix round (3 Important findings: presented-item dedup, policy state locking, pet-state serialization with the one-shot gate). The voice pass passed after two fix rounds.

## What Task 15 delivers

- `INotificationService` in Core (the only Core addition) and an `INotificationSink` seam in `src/Dudu.App/Notifications` with `AppNotificationService` (privacy-constrained translator) and `WindowsAppNotificationSink` (Windows App SDK, guarded by `OperatingSystem.IsWindows()`).
- `PresentationPolicy` (durable queue, ambient discard, one release per decision, 90 s minimum silent interval, duplicate rejection, GUID-only remote-note IDs that are never echoed) and `PresentationCoordinator` (the single gateway; tracks queued and currently-presenting keys; shares a `SemaphoreSlim` with `PetPresentationCoordinator` so explicit and unsolicited pet mutations never interleave).
- `ReminderDueSink` replaces the Infrastructure null sink via DI in `WindowsCompanionProductionComposition`; `AppHost` owns gateway lifecycle and ticks it after every successful reminder tick on the existing 30 s scheduler.
- Toast activation parses `action=open-note&messageId=<guid>` / `action=reminder-done|reminder-snooze&reminderId=<id>` and navigates to the Love Notes or Reminders page. Executing Done/Snooze from a toast is deferred (ledger: Task 15 minor deferred).
- Remote-note toasts are title-only `A note arrived 💌`; reminder toasts carry the reminder title plus `Done`/`Snooze` buttons. Registration failure flips `NotificationsAvailable` and the pet bubble remains the fallback.
- Harness scenario `--scenario notifications` (Windows-only).

Parked for the final whole-branch review: duplicated key=value parsing in `NotificationActivation`/`WindowsAppNotificationSink`; `NotificationsAvailable` never recovers after one failure (spec-aligned); `CaptureEnvironment` reads `_pet.Current` outside the pet gate (atomic reference read).

## What the voice pass delivers

All user-facing display text now follows `.superpowers/sdd/.../voice-guide.md` (measured from the owner's own texting corpus): lowercase, no trailing punctuation, short lopsided strings, signature vocabulary (u/ur, ah/le/de, oki/okkk/otayyy/yayyy, aiyo/alala, rest rest, good job, mwamwa, lovuh, namnam, isok, tmrw), rotating openers with `aiyo` capped at 20 across the diff. Scope: XAML pages/windows, view models, page code-behinds, overlay labels and guidance, tray menu, `PetStateMachine` bubbles, `LocalReminderDefaults`, `FocusService` user-reachable messages, `XamlCompileStubs` nav labels, and the 12 `SeedData` default love-note bodies.

Unchanged by design: `A note arrived 💌` (spec constant), all AutomationIds/x:Name/Tag/x:Key, product name `Dudu Desktop`, hotkey strings, log/protocol/internal exception text, everything under `Presentation/` and `Notifications/`, and the unused `SettingsShellViewModel` title array. Tests asserting visible strings (`SettingsNavigationTests`, `XamlContractTests`, `ActionBubbleLayoutTests`) were updated in lockstep. Parked: an empty-tasks state has no UI slot yet; one dead 9-word string in `ActionBubbleLayout`.

The voice guide is the style authority for any future copy. Its source corpus is private chat; never paste corpus lines into the app.

## Verification at the handoff

Run from the repository root with `./work/dotnet-sdk/dotnet`. The macOS compile path for App, App.Tests, UiTests and the harness needs these flags (without the last four the Windows-only XAML compiler step fails):

```
--no-restore -p:RuntimeIdentifier=win-x64 -p:PlatformTarget=x64 -p:WindowsAppSDKSelfContained=false -p:WindowsPackageType=None -p:AppxGeneratePriEnabled=false -p:GenerateAppInstallerFile=false -p:AppxPackageSigningEnabled=false -p:EnableCoreMrtTooling=false -p:ExpandPriResources=false -p:EnableDefaultApplicationDefinition=false -p:EnableDefaultPageItems=false
```

- `Dudu.Core.Tests`: 58 passed, 0 failed.
- `Dudu.Infrastructure.Tests`: 40 passed, 0 failed, 4 skipped (DPAPI requires Windows). Includes two new AppHost gateway lifecycle tests.
- `Dudu.App`, `Dudu.App.Tests`, `Dudu.UiTests`, `Dudu.WindowsHarness`: packaging-disabled stub builds with 0 warnings and 0 errors.
- `git diff --check`: clean.

Known quirk: after the RID-specific App builds, `dotnet test` for Core/Infrastructure can fail with `FileNotFoundException` from stale shared artifacts. Delete `obj/` and `bin/` under `src/Dudu.Core`, `src/Dudu.Infrastructure`, `tests/Dudu.Core.Tests`, `tests/Dudu.Infrastructure.Tests` and rerun.

Windows 11 x64 verification items: toast registration/display/activation, the `notifications` harness scenario (including the disable-notifications pet-bubble fallback check), real lock/fullscreen/quiet-hours suppression under the live scheduler, FlaUI UI tests, and a visual pass over the lowercase copy for layout fit.

## Repository hygiene and recovery notes

- Expected working-tree noise is limited to ignored/untracked `outputs/` and `work/` artifacts.
- Builds regenerate source-tree `packages.lock.json` files. Keep them untracked and move them back to `work/generated-package-locks/<dir-with-dashes>/`.
- The quarantine stashes and `codex/task14-*` / `codex/quarantine-*` refs described in the Task 14 handoff still exist. Do not apply, merge, or delete them.
- SDD workspace: `.superpowers/sdd/2026-09-11-dudu-desktop-companion-implementation/` (git-ignored). `progress.md` is the ledger; Task 15 and voice-pass briefs, reports, reviews and rulings live beside it.

## Start Task 16 here

Task 16 begins at line 1838 of the implementation plan: protocol JSON schemas, `EncryptedEnvelope`/`EnvelopeCrypto`/`DesktopKeyService` in Infrastructure, the TypeScript relay crypto, and the `Dudu.CryptoInterop` tool. It consumes `ISecretStore` and `ProductInfo.ProtocolVersion`. Remote-note IDs must be GUIDs in "D" format so `PresentationCoordinator` accepts them (Task 15 ruling 5).

## Agent execution constraint

Subagent-driven development with Sonnet implementers and reviewers first; Opus only for genuinely cross-layer work or after a bounded Sonnet attempt fails (user mapping: luna→sonnet, terra→opus).
