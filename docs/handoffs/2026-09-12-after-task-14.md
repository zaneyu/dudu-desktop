# Dudu Windows handoff after Task 14

Date: 2026-09-12  
Branch: `codex/windows-v1`  
Task 14 implementation head: `0f5d87a` (`fix: bind pointer dispatch to presented actions`)  
Next planned work: Task 15, private notifications and the unified presentation-suppression gateway

## Current status

Tasks 1 through 14 in `docs/superpowers/plans/2026-09-11-dudu-desktop-companion-implementation.md` are complete. Do not restart or reimplement Task 14.

Task 14's final Luna max review returned `APPROVED` with no Critical or Important findings. A preceding Terra audit was used only after broad Luna reviewers stalled on the cross-layer native concurrency check. It found three Important defects, all fixed in `0f5d87a`:

1. Native clicks now use the action identity and hit geometry belonging to the exact last-presented frame, including across DPI/scale reflow.
2. `Close` cancels an active breathing exercise before waiting behind serialized pointer dispatch.
3. Overlay-host disposal cancels and waits for the owned pointer-dispatch queue with a bounded timeout and diagnostics.

## What Task 14 delivers

- Seven functional WinUI settings pages: Home, Reminders, Tasks and Focus, Love Notes, Appearance, Connection, and Privacy and Data.
- Reminder, task, focus, countdown, manual check-in, note, appearance, pairing, backup, restore, and deletion workflows wired through application services.
- Explicit two-step confirmation for destructive restore/local-delete/remote-delete actions.
- Explicit remote-note reveal and separate save-to-jar behavior; selecting an envelope does not reveal its contents.
- A complete local-data inventory covering retention, backup inclusion, deletion, and whether each entity leaves the PC.
- Immediate live application of a successfully persisted theme to the settings shell and overlay preferences.
- A no-activate native action surface with six primary actions and exactly five comfort actions, equivalent accessible Settings routes, bounded/reflowed layout, reserved and clipped guidance/error text, and DPI-safe painted/clickable geometry.
- Serialized, exception-observed pointer commands; cancellable breathing; stale-completion protection; coalesced repaints; and serialized animation-pack replacement.
- Isolated Windows UI automation data roots and instance names. The end-to-end journey creates/completes a reminder and task, starts/ends focus, records a check-in, exercises all accessible overlay routes, and verifies pause state.

## Verification at the handoff

Run from the repository root with `./work/dotnet-sdk/dotnet`:

- `Dudu.Core.Tests`: 58 passed, 0 failed.
- `Dudu.Infrastructure.Tests`: 38 passed, 0 failed, 4 skipped because DPAPI requires Windows.
- `Dudu.App.Tests`: packaging-disabled Windows-targeted build passed with 0 warnings and 0 errors.
- `Dudu.UiTests`: packaging-disabled Windows-targeted build passed with 0 warnings and 0 errors.
- `Dudu.App` is built transitively by both test-project builds with 0 warnings and 0 errors.
- `git diff --check`: passed before the Task 14 fix commit.

The macOS host can compile the Windows target but cannot honestly execute WinUI, FlaUI, the Windows App SDK XAML compiler/runtime path, native HWND behavior, or DPAPI. These remain Windows 11 x64 verification items, not hidden failures.

On Windows 11, run at minimum:

```powershell
dotnet test DuduDesktop.slnx -c Release
dotnet test tests/Dudu.UiTests/Dudu.UiTests.csproj --filter FullyQualifiedName~SettingsNavigationTests
```

Provide `DUDU_UI_TEST_EXE` when the UI test runner is not using its default app location. Also manually exercise native action clicks while changing display DPI/pet scale and close the comfort panel during an active full-motion breathing cycle.

## Repository hygiene and recovery notes

- Expected working-tree noise is limited to ignored/untracked `outputs/` and `work/` artifacts.
- Builds regenerate source-tree `packages.lock.json` files. Keep them untracked and move them back to matching directories under `work/generated-package-locks/`; do not commit them unless the plan is deliberately changed.
- Several stashes contain contaminated or stalled Task 14 drafts. Do not apply them wholesale:
  - `quarantined local Privacy page draft`
  - `quarantined final delayed Home sync residue`
  - `quarantined delayed subagent sync residue`
  - `discarded stalled Luna round2 residue`
  - reviewer residue stashes
- `codex/quarantine-delayed-sync-4ce956c` preserves a stale delayed-agent branch state.
- Temporary Task 14 refs may still exist: `codex/task14-candidate`, `codex/task14-audit`, `codex/task14-candidate2`, and `codex/task14-review-temp`. Do not delete or merge them without inspecting them first.
- The canonical implementation is the current `codex/windows-v1` history, not any stash or temporary Task 14 ref.

## Start Task 15 here

Task 15 begins at line 1750 of the implementation plan. Follow its test-first order.

Create:

- `src/Dudu.Core/Abstractions/INotificationService.cs`
- `src/Dudu.App/Notifications/AppNotificationService.cs`
- `src/Dudu.App/Presentation/PresentationPolicy.cs`
- `src/Dudu.App/Presentation/PresentationCoordinator.cs`
- `src/Dudu.App/Presentation/ReminderDueSink.cs`
- `tests/Dudu.App.Tests/Presentation/PresentationPolicyTests.cs`
- `tests/Dudu.App.Tests/Notifications/NotificationPrivacyTests.cs`

Modify `src/Dudu.App/Hosting/AppHost.cs` for composition and lifecycle ownership.

Required contracts and invariants:

- Every unsolicited event passes through one `PresentationCoordinator`; do not add a second suppression path.
- Queue durable remote-note and non-bypass reminder events during quiet hours, focus, fullscreen, session lock, or pause. Discard ambient events rather than queueing them.
- Release at most one durable event after suppression ends, then enforce the configured minimum silent interval.
- Explicit user actions remain immediate.
- `ReminderDueSink` converts a committed reminder occurrence into `PetEvent.ReminderDue` plus a notification request while keeping Windows dependencies out of `Dudu.Core`.
- `ShowRemoteNoteArrivalAsync` accepts only a protocol-safe message ID. It must never accept, log, render, or include plaintext/ciphertext. The notification is title-only: `A note arrived 💌`.
- Reminder notifications may include the reminder title and `Done`/`Snooze` activation arguments.
- Failed/unavailable Windows notification registration must fall back to the pet bubble without losing the durable event.
- Preserve the Task 14 privacy controls and explicit reveal boundary.

The first two required tests are the remote-notification privacy test and the “leaving quiet hours releases one durable item, not a burst” policy test shown in the plan. Finish with the Windows notification harness scenario and commit the milestone as `feat: enforce private calm presentation policy`.

## Agent execution constraint

Continue subagent-driven implementation with `gpt-5.6-luna` at max effort first. Use `gpt-5.6-terra` only when the work is genuinely too cross-layer or a bounded Luna attempt has failed; keep Terra usage narrow and explicit.
